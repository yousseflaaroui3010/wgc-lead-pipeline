node_modules/
*.log
.env
infra/.env
infra/nginx/hook_id.conf

# SSH keys must never be committed
infra/newssh
infra/newssh.pub
*.pem
id_*

# Client business data stays out of git (full unit addresses)
data/
# Derived from the lease data (rents by segment) — same business-data rule.
# Regenerated from the CSV via `npm run estimate:index`; mounted into n8n.
estimator/dist/
